version: 2

models:
  - name: renzo_pool_swaps_metrics
    meta:
      experts:
        users:
          - anton@connext.network
    config:
      y42:
        apiVersion: v1
      tags:
        - dashboard
