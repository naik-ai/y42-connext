version: 2

models:
  - name: slow_path_volume__hourly
    meta:
      experts:
        users:
          - jay@naik.ai
      tier: tier 2
    config:
      y42:
        apiVersion: v1
      tags:
        - snapshot
